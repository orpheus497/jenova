#!/bin/sh
# Auto-generated by bin/build-llama-jenova on 2026-04-07 09:42:50 UTC
# Host-specific overrides for llama.cpp runtime in Jenova.

LLAMA_SERVER="$JENOVA_ROOT/llama.cpp/build/bin/llama-server"
THREADS="${JENOVA_THREADS:-6}"
THREADS_BATCH="${JENOVA_THREADS_BATCH:-8}"
NGL_7B="${JENOVA_NGL_7B:-all}"
FIT_TARGET="${JENOVA_FITT:-768}"
KV_CACHE_TYPE="${JENOVA_KV_TYPE:-q8_0}"
DEVICES="${JENOVA_DEVICES:-Vulkan0}"
TENSOR_SPLIT="${JENOVA_TS:-}"
# Allow Vulkan backend to fallback to system memory under pressure.
GGML_VK_ALLOW_SYSMEM_FALLBACK="${GGML_VK_ALLOW_SYSMEM_FALLBACK:-1}"
