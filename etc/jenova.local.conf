#!/bin/sh
# Auto-generated by bin/build-llama-jenova on 2026-04-07 08:39:39 UTC
# Host-specific overrides for llama.cpp runtime in Jenova.

LLAMA_SERVER="$JENOVA_ROOT/llama.cpp/build/bin/llama-server"
THREADS="${JENOVA_THREADS:-6}"
THREADS_BATCH="${JENOVA_THREADS_BATCH:-8}"
NGL_7B="${JENOVA_NGL_7B:-16}"
FIT_TARGET="${JENOVA_FITT:-768}"
KV_CACHE_TYPE="${JENOVA_KV_TYPE:-q8_0}"
DEVICES="${JENOVA_DEVICES:-CUDA0}"
# Allow CUDA to overflow VRAM into system RAM instead of hard OOM.
GGML_CUDA_ENABLE_UNIFIED_MEMORY="${GGML_CUDA_ENABLE_UNIFIED_MEMORY:-1}"
