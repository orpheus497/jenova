#!/bin/sh
# Hardware Profile: HP Laptop 15s-eq2xxx — FreeBSD 15 | AMD Ryzen 7 5700U | AMD Lucienne
# PLACEHOLDER — researched optimal settings, not yet validated on hardware.

PROFILE_NAME="freebsd-ryzen7-5700u-amd"
PROFILE_DESC="FreeBSD 15 | AMD Ryzen 7 5700U | AMD Lucienne (Vega 8) | 15.28 GiB | ZFS"

# --- Detection patterns (used by detect-hardware.sh) ---
MATCH_CPU="Ryzen 7 5700U"
MATCH_GPU_0="Lucienne\|Renoir\|Vega.*8\|RADV RENOIR"
MATCH_GPU_1=""
MATCH_OS="FreeBSD"

# --- Hardware summary ---
HW_CPU="AMD Ryzen 7 5700U (8C / 16T) @ 1.80 GHz base, 4.3 GHz boost"
HW_GPU_0="AMD Radeon RX Vega 8 (Lucienne) — UMA, 8 CUs / 512 shaders, shares system RAM"
HW_GPU_1=""
HW_RAM="15.28 GiB"
HW_SWAP="16 GiB"
HW_STORAGE="ZFS (423.91 GiB root)"
HW_GPU_TOTAL_VRAM="~2-4 GiB UMA (allocated from system RAM by BIOS/firmware)"

# --- Inference strategy ---
STRATEGY="single-gpu-partial-offload"
STRATEGY_DESC="AMD Lucienne (Vega 8) is UMA — GPU VRAM is carved from system RAM. With ~15 GiB total and ~2-4 GiB allocatable to GPU, partial layer offload (14-18 of 28 layers) is optimal. Remaining layers stay on CPU. The 8C/16T Zen 2 CPU provides strong parallel inference throughput for CPU-resident layers. ZFS swap handles overflow. No Optane — standard NVMe latency (~100us), so swap-heavy workloads are slower than Optane-equipped systems."

# --- Key config values ---
PROFILE_DEVICES="Vulkan0"
PROFILE_TENSOR_SPLIT=""
PROFILE_FIT_TARGET="512"
PROFILE_CTX_SIZE="8192"
PROFILE_NUM_SLOTS="2"
PROFILE_THREADS="8"
PROFILE_THREADS_BATCH="12"
PROFILE_NGL="18"
PROFILE_KV_TYPE="q8_0"
