#!/bin/sh
# Hardware Profile: FreeBSD i5-1135G7 Dual-GPU — 3B model
# This file describes the hardware profile for auto-detection matching.

PROFILE_NAME="FreeBSD/dgpu_igpu/i5-1135g7-3b"
PROFILE_DESC="FreeBSD 15 | Intel i5-1135G7 | GTX 1650 Ti + Iris Xe | 16GB | Qwen3.5-4B Q8_0"

# --- Detection patterns (used by detect-hardware.sh) ---
MATCH_CPU="i5-1135G7"
MATCH_GPU_0="NVIDIA"
MATCH_GPU_1="Intel.*(Iris|Xe)"
MATCH_OS="FreeBSD"

# --- Hardware summary ---
HW_CPU="Intel i5-1135G7 (4P / 8T)"
HW_GPU_0="NVIDIA GTX 1650 Ti 4GB (Vulkan0) — discrete VRAM"
HW_GPU_1="Intel Iris Xe TGL GT2 (Vulkan1) — UMA, ~7 GiB from system RAM"
HW_RAM="16 GiB"
HW_SWAP="Any (not required — 3B model fits easily in GPU)"
HW_STORAGE="ZFS"
HW_GPU_TOTAL_VRAM="~11 GiB combined (4 GiB discrete + ~7 GiB UMA)"

# --- Inference strategy ---
STRATEGY="dual-gpu-full-offload-3b"
STRATEGY_DESC="Qwen2.5-Coder-3B Q8_0 (~3.5 GiB) fully offloaded across both Vulkan GPUs (~11 GiB combined). All 36 layers on GPU. ~8 GiB headroom available for 32K context + KV cache + 0.5B drafter. No swap-backed filesystem required."

# --- Key config values (should match jenova.conf in this directory) ---
PROFILE_DEVICES="Vulkan0,Vulkan1"
PROFILE_TENSOR_SPLIT=""
PROFILE_FIT_TARGET="512"
PROFILE_CTX_SIZE="32768"
PROFILE_NUM_SLOTS="2"
PROFILE_THREADS="4"
PROFILE_THREADS_BATCH="6"
PROFILE_NGL="all"
PROFILE_KV_TYPE="q8_0"
PROFILE_DRAFT="1"
PROFILE_HEALTH_TIMEOUT="60"
