#!/bin/sh
# Hardware Profile: Generic Vulkan Full Offload — 14B model
# This file describes the hardware profile for auto-detection matching.

PROFILE_NAME="vulkan-full-offload"
PROFILE_DESC="Generic Vulkan GPU with 8GB+ VRAM — Qwen2.5-14B Q4_K_M full offload"

# --- Detection patterns (used by detect-hardware.sh) ---
# Catch-all for any Vulkan-capable GPU. Scores lower than specific profiles.
MATCH_GPU_0=".*(NVIDIA|GeForce|Quadro|RTX|GTX|AMD|ATI|Radeon|RX|Intel|Arc).*"
MATCH_OS=""       # Any OS with Vulkan support

# --- Hardware summary ---
HW_CPU="Generic multi-core CPU (4+ cores recommended)"
HW_GPU_0="Vulkan-capable GPU with 8GB+ VRAM (NVIDIA RTX/AMD RX/Intel Arc)"
HW_RAM="16+ GiB recommended"
HW_SWAP="Optional — not required with sufficient VRAM"
HW_GPU_TOTAL_VRAM="8+ GiB (10+ GiB recommended for 14B Q4_K_M with default context)"

# --- Inference strategy ---
STRATEGY="full-offload"
STRATEGY_DESC="Qwen2.5-Coder-14B Q4_K_M (~8.7 GiB) fully offloaded to a single Vulkan GPU. Requires 10+ GiB VRAM for 16k context + KV cache + 0.5B drafter. On strict 8 GiB GPUs, reduce to CTX=8192 SLOTS=1. Best quality Jenova agent on single-GPU hardware. Suitable for NVIDIA RTX 3080/4070/4080/4090, AMD RX 7900, Intel Arc A770 (16GB)."

# --- Key config values (should match jenova.conf in this directory) ---
PROFILE_DEVICES="Vulkan0"
PROFILE_TENSOR_SPLIT=""
PROFILE_FIT_TARGET="1024"
PROFILE_CTX_SIZE="16384"
PROFILE_NUM_SLOTS="2"
PROFILE_THREADS="4"
PROFILE_THREADS_BATCH="6"
PROFILE_NGL="all"
PROFILE_KV_TYPE="q8_0"
PROFILE_DRAFT="1"
