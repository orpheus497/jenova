# jenova.conf: Generic Profile Template
#
# Hardware-aware configuration with auto-discovery.

# --- Paths ---
if [ -z "$JENOVA_ROOT" ]; then
    _EXE=$(realpath "$0")
    _DIR=$(dirname "$_EXE")
    if [ -f "$_DIR/etc/jenova.conf" ]; then
        _ROOT="$_DIR"
    elif [ -f "$(dirname "$_DIR")/etc/jenova.conf" ]; then
        _ROOT=$(dirname "$_DIR")
    else
        _ROOT=$(dirname "$(dirname "$(dirname "$(dirname "$_DIR")")")")
    fi
    JENOVA_ROOT="$_ROOT"
fi
JENOVA_HOME="${JENOVA_HOME:-$HOME/Jenova}"; export JENOVA_HOME

# --- Model Auto-Discovery ---
# shellcheck disable=SC1091
. "$JENOVA_ROOT/lib/jenova-model.sh"

# --- Models ---
# Models are auto-discovered by lib/jenova-model.sh.
# Environment variables (JENOVA_MODEL, JENOVA_DRAFT_MODEL, JENOVA_EMBED_MODEL)
# take precedence if set.
MODEL_PATH="${JENOVA_MODEL:-$MODEL_AGENT}"

# Hardware offload (defaults)
DEVICES="${JENOVA_DEVICES:-Vulkan0}"
NGL_AGENT="${JENOVA_NGL_AGENT:-all}"
CTX_SIZE="${JENOVA_CTX:-16384}"
JENOVA_DRAFT="${JENOVA_DRAFT:-1}"

# Threading
THREADS="${JENOVA_THREADS:-4}"
THREADS_BATCH="${JENOVA_THREADS_BATCH:-4}"
