#!/bin/sh
# Hardware Profile: Generic NVIDIA CUDA Full Offload
# This file describes the hardware profile for auto-detection matching.

PROFILE_NAME="CUDA/dgpu/nvidia-generic"
PROFILE_DESC="Generic NVIDIA GPU with CUDA support — Optimized for RTX/GTX series"

# --- Detection patterns (used by detect-hardware.sh) ---
# Scores higher than Vulkan profiles on systems with NVIDIA GPUs due to glob order
# and specificity.
MATCH_GPU_0="NVIDIA|GeForce|Quadro|RTX|GTX"
MATCH_OS="Linux|FreeBSD"

# --- Hardware summary ---
HW_CPU="Generic multi-core CPU"
HW_GPU_0="NVIDIA GPU with CUDA support"
HW_RAM="16+ GiB recommended"

# --- Inference strategy ---
STRATEGY="cuda-full-offload"
STRATEGY_DESC="Full offload to NVIDIA GPU via CUDA backend. Optimized for performance and low latency."

# --- Key config values (should match jenova.conf in this directory) ---
PROFILE_DEVICES="CUDA0"
PROFILE_TENSOR_SPLIT=""
PROFILE_FIT_TARGET="768"
PROFILE_CTX_SIZE="32768"
PROFILE_NUM_SLOTS="2"
PROFILE_THREADS="4"
PROFILE_NGL="all"
PROFILE_KV_TYPE="q8_0"
PROFILE_DRAFT="1"
