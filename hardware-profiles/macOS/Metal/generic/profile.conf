#!/bin/sh
# Hardware Profile: macOS Metal — Generic Apple Silicon/Metal GPU
# This file describes the hardware profile for auto-detection matching.

PROFILE_NAME="macOS/Metal/generic"
PROFILE_DESC="macOS Apple Silicon — Metal GPU acceleration with Qwen2.5-3B Q8_K_M"

# --- Detection patterns (used by detect-hardware.sh) ---
MATCH_OS="Darwin"
MATCH_GPU_0="Apple.*M|Apple.*GPU|Metal"

# --- Hardware summary ---
HW_CPU="Apple Silicon (M1/M2/M3/M4 series)"
HW_GPU_0="Apple Silicon integrated GPU (Metal)"
HW_GPU_1=""
HW_RAM="8+ GiB"
HW_SWAP="macOS unified memory"
HW_STORAGE="APFS"
HW_GPU_TOTAL_VRAM="Shared with system RAM (unified memory)"

# --- Inference strategy ---
STRATEGY="metal-full-offload-3b"
STRATEGY_DESC="Qwen2.5-3B-Instruct-Q8_K_M optimized for Apple Silicon Metal. Full GPU offload using Metal Performance Shaders. Unified memory architecture provides efficient CPU↔GPU data transfer. Suitable for M1/M2/M3/M4 Macs with 8GB+ RAM."

# --- Key config values ---
PROFILE_DEVICES="Metal0"
PROFILE_TENSOR_SPLIT=""
PROFILE_FIT_TARGET="512"
PROFILE_CTX_SIZE="16384"
PROFILE_NUM_SLOTS="2"
PROFILE_THREADS="4"
PROFILE_THREADS_BATCH="8"
PROFILE_NGL="all"
PROFILE_KV_TYPE="q8_0"
PROFILE_DRAFT="1"
PROFILE_HEALTH_TIMEOUT="60"

# --- Model Recommendations ---
RECOMMENDED_AGENT_MODEL="Qwen2.5-3B-Instruct-Q8_K_M.gguf"
RECOMMENDED_AGENT_URL="https://huggingface.co/Qwen/Qwen2.5-3B-Instruct-GGUF/resolve/main/qwen2.5-3b-instruct-q8_k_m.gguf"
RECOMMENDED_EMBED_MODEL="Qwen3-Embedding-0.6B-Q8_0.gguf"
RECOMMENDED_EMBED_URL="https://huggingface.co/Qwen/Qwen3-Embedding-0.6B-GGUF/resolve/main/qwen3-embedding-0.6b-q8_0.gguf"