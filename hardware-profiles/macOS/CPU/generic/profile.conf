#!/bin/sh
# Hardware Profile: macOS CPU — Apple Silicon CPU-only inference
# This file describes the hardware profile for auto-detection matching.

PROFILE_NAME="macOS/CPU/generic"
PROFILE_DESC="macOS Apple Silicon CPU-only — Qwen3.5-0.8B Q8 for agent and embedding"

# --- Detection patterns (used by detect-hardware.sh) ---
MATCH_OS="Darwin"
# No GPU match = CPU-only profile

# --- Hardware summary ---
HW_CPU="Apple Silicon (M1/M2/M3/M4 series)"
HW_GPU_0="None (CPU-only)"
HW_GPU_1=""
HW_RAM="8+ GiB"
HW_SWAP="macOS unified memory paging"
HW_STORAGE="APFS"
HW_GPU_TOTAL_VRAM="N/A (CPU-only)"

# --- Inference strategy ---
STRATEGY="cpu-only-0.8b"
STRATEGY_DESC="Qwen3.5-0.8B-Instruct-Q8 optimized for Apple Silicon CPU-only inference. Uses Neural Engine acceleration where available. Suitable for M1/M2/M3/M4 Macs with limited RAM or when GPU acceleration is not desired. Compact model fits in CPU cache for fast inference."

# --- Key config values ---
PROFILE_DEVICES="CPU"
PROFILE_TENSOR_SPLIT=""
PROFILE_FIT_TARGET="128"
PROFILE_CTX_SIZE="8192"
PROFILE_NUM_SLOTS="1"
PROFILE_THREADS="4"
PROFILE_THREADS_BATCH="4"
PROFILE_NGL="0"
PROFILE_KV_TYPE="q8_0"
PROFILE_DRAFT="0"
PROFILE_HEALTH_TIMEOUT="30"

# --- Model Recommendations ---
RECOMMENDED_AGENT_MODEL="Qwen3.5-0.8B-Instruct-Q8.gguf"
RECOMMENDED_AGENT_URL="https://huggingface.co/Qwen/Qwen3.5-0.8B-Instruct-GGUF/resolve/main/qwen3.5-0.8b-instruct-q8.gguf"
RECOMMENDED_EMBED_MODEL="Qwen3.5-0.8B-Instruct-Q8.gguf"
RECOMMENDED_EMBED_URL="https://huggingface.co/Qwen/Qwen3.5-0.8B-Instruct-GGUF/resolve/main/qwen3.5-0.8b-instruct-q8.gguf"